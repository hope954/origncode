PORT=3000
DATA_FILE=./data/store.json
# 32-byte base64 key preferred; dev fallback exists but replace in real deployment.
TOKEN_ENCRYPTION_KEY=change_me_for_prod

# Stage 4 real integration (planned / optional until real platform HTTP is enabled)
FEISHU_APP_ID=
FEISHU_APP_SECRET=
FEISHU_REDIRECT_URI=
# Optional override for private-region / gateway scenarios
FEISHU_BASE_URL=https://open.feishu.cn
YUQUE_BASE_URL=https://www.yuque.com/api
